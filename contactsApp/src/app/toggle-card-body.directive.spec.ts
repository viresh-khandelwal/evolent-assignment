import { ToggleCardBodyDirective } from './toggle-card-body.directive';

describe('ToggleCardBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleCardBodyDirective();
    expect(directive).toBeTruthy();
  });
});
